<Window x:Class="PL.Task.TaskListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Task"
        mc:Ignorable="d"
        Title="TaskListWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <ListView Margin="5" Grid.Row="1" ItemsSource="{Binding TasksList}">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <ComboBox ItemsSource="{Binding Source={StaticResource WorkerExperienceCollectionKey}}" RenderTransformOrigin="0.476,0.473" Margin="288,0,0,386" SelectedValue="{Binding Path=Complexity, Mode=TwoWay}" SelectionChanged="FilterListByComplexity"/>

    </Grid>
</Window>
