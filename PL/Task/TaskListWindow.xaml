<Window x:Class="PL.Task.TaskListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Task"
        mc:Ignorable="d"
        Title="TaskListWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid Margin="131,0,0,108">
        <Grid.RowDefinitions>
            <RowDefinition Height="111*"/>
            <RowDefinition Height="52*"/>
        </Grid.RowDefinitions>
        <ListView Margin="5,5,5,5" Grid.Row="1" MouseDoubleClick="UpdateTask" SelectionChanged="ListView_SelectionChanged" Grid.RowSpan="2">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Add" Click="AddTask" Margin="252,342,0,10" Grid.Row="1"/>
        <ComboBox Grid.Column="1"   ItemsSource="{Binding Source={StaticResource WorkerExperienceCollectionKey}}" RenderTransformOrigin="0.476,0.473" Margin="190,0,0,0" SelectedValue="{Binding Path=Complexity, Mode=TwoWay}" SelectionChanged="FilterListByComplexity"/>

    </Grid>
</Window>
